# Argo CD Inside vCluster with Virtual Cluster Templates

Creates a virtual cluster with a `VirtualClusterTemplate` that automatically adds the virtual cluster to the Argo CD instance configured with the vCluster Platform `Project` where the virtual cluster is created.
